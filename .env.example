# 环境变量示例。
#
# 请勿把真实的 .env 提交到仓库（已在 .gitignore 中排除）。
# 这个文件本身只放占位符，可以安全提交。

# LLM API Key（必填，除非只跑离线求解与测试）
# 到 https://www.siliconflow.cn/ 注册后可获取
SILICONFLOW_API_KEY=your_api_key_here

# 运行日志路径，默认 learning_log.txt；设为 off 可完全关闭日志
# ADPLANNER_LOG=off

# 可选：指定 Python 解释器（Windows 默认 python，其他平台默认 python3）
# 该解释器需要已安装 ortools 与 requests
# ADPLANNER_PYTHON=python
